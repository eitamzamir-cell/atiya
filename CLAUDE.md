# עטיה — מדריך לקלוד

הקובץ הזה נועד לקלוד. כל שיחה חדשה מתחילה בלי זיכרון מהשיחות הקודמות —
תקרא את הקובץ הזה **לפני** שאתה נוגע במשהו, והוא ייתן לך את כל ההקשר.

## מה זה
חנות אונליין של עטיה — מטפחות ראש ומפות וינטג׳. הבעלים: איתם ויסכה.
האתר חי בכתובת **https://atiya.online** (נטליפיי, פריסה אוטומטית מ-GitHub).

## מבנה המאגר — שים לב, זה לא סטנדרטי
בגלל תקלת העלאה היסטורית, **כל הפרויקט יושב בתוך `upload1/`**:

```
upload1/
├── public/                  ← האתר. זו תיקיית הפרסום
│   ├── index.html           ← כל האתר בקובץ אחד: HTML + CSS + JS
│   ├── stock.html           ← עמוד ניהול מלאי (מוגן בסיסמה)
│   ├── thanks.html          ← נחיתה אחרי תשלום מוצלח
│   ├── payment-error.html   ← נחיתה אחרי תשלום שנכשל
│   ├── img/                 ← 92 תמונות
│   └── robots.txt, sitemap.xml, אייקונים
├── netlify/functions/
│   ├── create-payment.js    ← חתימה מול Hyp, חישוב סכום, שריון מלאי
│   ├── stock.js             ← המלאי החי שהאתר קורא
│   ├── stock-admin.js       ← צפייה ועדכון מלאי (דורש סיסמה)
│   ├── _stock.js            ← שכבת האחסון (Netlify Blobs)
│   └── catalog.json         ← מחירים ומלאי התחלתי
├── package.json
└── netlify.toml
```
`upload2/` בשורש המאגר היא שארית מיותרת. היא מחוץ לתיקיית הפרסום ואינה משפיעה. אל תשקיע בה.

הגדרות הבנייה בנטליפיי: Base directory = `upload1`, Build command = `npm install`,
Publish directory = `public`.

## ⚠️ הכלל הכי חשוב: מחירים יושבים בשני מקומות
שינוי מחיר חייב להתבצע **בשניהם**, אחרת השרת יגבה סכום שונה ממה שהלקוחה רואה:
1. `upload1/public/index.html` — במערך `PRODUCTS`
2. `upload1/netlify/functions/catalog.json`

השרת מחשב את הסכום מ-`catalog.json` בלבד ומתעלם ממה שהדפדפן שולח — זו הגנה מכוונת.

## כללים נוספים
- **משלוח:** 30 ₪, חינם מעל 300 ₪. מוגדר בשני מקומות: `SHIPPING_FEE`/`FREE_OVER`
  ב-`index.html`, ואותם קבועים בתוך `create-payment.js`. לשנות בשניהם.
- **איסוף עצמי:** סנסנה או תקוע, ללא עלות. הלקוחה בוחרת בטופס ההזמנה
  (`name="delivery"`), והדפדפן שולח `delivery: 'pickup'`. **השרת מאפס את דמי
  המשלוח בעצמו** לפי הערך הזה — אל תסמוך על סכום שמגיע מהדפדפן.
- **מלאי שוטף** מנוהל דרך `/stock.html` ונשמר ב-Netlify Blobs. אין צורך לגעת בקוד.
  `catalog.json` מגדיר רק את ערך ההתחלה.
- **תמונות** מוטמעות כקבצים ב-`img/` עם `loading="lazy"`. אל תחזיר data-URI לתוך
  ה-HTML — זה מה שהאט את האתר פי עשרה בעבר.
- **וואטסאפ:** הקבוע `WHATSAPP` ב-`index.html`. כרגע `972587720013`.
- **אל תכניס סודות ל-HTML.** כל המפתחות יושבים במשתני סביבה בנטליפיי.

## משתני סביבה (Netlify → Project configuration → Environment variables)
| שם | תפקיד |
|---|---|
| `HYP_MASOF`, `HYP_KEY`, `HYP_PASSP` | סליקה מול Hyp |
| `NETLIFY_API_TOKEN` | נדרש לאחסון המלאי — בלעדיו המלאי לא נשמר |
| `ATYA_ADMIN_KEY` | סיסמת עמוד המלאי |

## תהליך התשלום
לקוחה ממלאת טופס → ההזמנה נרשמת ב-Netlify Forms ונשלחת למייל (עם מספר `ATY-`)
→ `create-payment` מחשב סכום, משריין מלאי וחותם מול Hyp → הפניה לדף התשלום
→ חזרה ל-`thanks.html`.
**ההזמנה נרשמת לפני התשלום**, אז ייתכן מייל הזמנה בלי תשלום בפועל. מצליבים מול
הפאנל של Hyp לפי מספר ההזמנה.

## איך לפרוס שינוי
`git push` למאגר. נטליפיי בונה ומפרסם לבד תוך דקה-שתיים. אין שלב ידני.

## מה עוד פתוח
- אין מייל אישור מהחנות ללקוחה (רק קבלה מ-Hyp)
- אין סימון אוטומטי של "שולם" — נדרשת הצלבה ידנית
- פריט שלקוחה נטשה בדף התשלום נשאר תפוס עד שחרור ידני ב-`/stock.html`
- לא הוגדרה חשבונית אוטומטית

## סגנון
האתר בעברית, RTL, פלטת פשתן וטרקוטה. שמור על הטון: חם, נקי, לא צעקני.
כל שינוי — לבדוק בפועל לפני שאומרים שהוא עובד.
